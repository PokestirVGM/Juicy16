//
// Single source of truth for the user-facing version string, shown in the UI so
// it's obvious when a new build has loaded. Bump this on every change handed over.
//

#pragma once

#define JUICYSF_RACK_VERSION "0.3.10"
